import { IRenderEngine } from "../../src/RenderEngine";

export default class RenderEngine implements IRenderEngine {
  public renderBixel = (x:number, y:number, colourValue:string):void => {
    //
  }
}
